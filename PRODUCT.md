# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

주 사용자는 경영진/의사결정권자입니다 — 창고 실무자가 아니라, 재고 상태를 빠르게 훑고 판단을 내려야 하는
사람이 일상적으로 이 대시보드를 봅니다. 이들은 "지금 무엇을 먼저 확인해야 하는가"를 최우선으로 원하며,
세부 데이터 입력보다 요약 지표·우선순위 신호를 스캔하는 데 시간을 씁니다. (Excel 업로드 자체는 창고
실무자가 수행하지만, 대시보드의 매일 사용자는 의사결정권자입니다.)

## Product Purpose

3개 창고(서로 다른 품목군을 관리하는 독립 재고 Pool)에서 매일 추출하는 재고 Excel 스냅샷을 업로드하면
소진 추세·Coverage·예상 소진일·소진 가속/둔화·장기 정체·과잉재고 후보·소비기한 임박 위험을 계산해
보여주는 사내 재고관리 대시보드입니다. 성공은 의사결정권자가 화면을 열자마자 "지금 조치가 필요한 SKU"를
가장 먼저 보고, 그 판단에 필요한 근거(추세·기준)를 바로 이어서 확인할 수 있는 상태입니다.

## Positioning

이 데이터는 **재고 Snapshot이며 판매 데이터가 아닙니다** — 모든 지표는 "추정 소진량", "관측된 재고
증가"처럼 스냅샷만으로 확인 가능한 사실만 표현하며, 실측되지 않은 값을 사실처럼 제시하지 않습니다.
위험/경고 수량 기준은 수동 설정 또는 재고소진주기 역산으로 자동 산출되고, 소비기한 위험도 SKU별
판정일수 설정과 커버리지 갭 계산으로 도출됩니다 — 표준 재고 대시보드처럼 정적 임계값을 나열하는 것이
아니라, 각 SKU의 실제 소진 패턴에 근거해 위험을 판단합니다.

## Operating Context

- 창고 A/B/C가 각각 독립적으로 매일(또는 비정기적으로) Excel 스냅샷을 업로드합니다.
- 입고 특이사항(상품명/코드 + 실제 입고 수량)을 기록하면 해당 구간의 추정 소진량을 보정합니다.
- 기준일 조회(과거 시점 재분석)와 기간 비교 조회를 모두 지원합니다.
- Action Center → KPI 카드 → 창고별 요약 → 차트 → 전체 재고 테이블 → SKU 상세 순으로, 요약에서
  세부로 내려가는 탐색 구조입니다.
- 게시판(메모/재고 이벤트 로그)이 별도로 존재합니다.

## Capabilities and Constraints

- Role 기반 접근: MEMBER(조회) / ADMIN(위험 기준·소비기한 판정일수 등 정책 설정 가능).
- 위험/경고 수량: 수동 설정 우선, 없으면 레거시 값, 그 다음 평균 일일 소진량 기반 자동 계산.
- 소비기한 위험: SKU별 판정일수(기본 14일) × 커버리지 갭 계산 — 실측 소진 이력(coverageDays)이
  없으면 위험으로 판정하지 않음(오탐 방지).
- 기술 스택: Next.js(App Router) + TypeScript, Tailwind CSS, 수작성 shadcn 스타일 UI 컴포넌트(Radix
  기반), Recharts, Prisma + PostgreSQL, Auth.js.
- 한국어 전용 UI (Noto Sans KR).

## Evidence on Hand

- README.md에 도메인 규칙(스냅샷 의미, 입고 보정 로직, mock 데이터 분리 등)이 상세히 문서화되어 있음.
- 실제 브랜드 자산(로고, 지정 컬러, 마케팅 자료)은 없음 — 사내 전용 도구로 자유로운 비주얼 방향 설정 가능.

## Product Principles

- 추정치를 사실처럼 포장하지 않는다 — 스냅샷 기반 추정과 실측을 항상 구분해 표시한다.
- 의사결정권자가 화면을 열자마자 "지금 확인해야 할 것"을 가장 먼저 보게 한다 — 요약에서 세부로
  내려가는 탐색 구조를 유지한다.
- 위험 판정은 정적 임계값이 아니라 SKU별 실제 소진 패턴에 근거한다.
- 데이터 입력(업로드)은 실무자의 일이고, 일상적 열람은 의사결정권자의 일이다 — 두 역할의 화면
  우선순위가 다를 수 있음을 인지한다.


## Confirmed operating policy (2026-09-19)

주말·공휴일에는 출고가 없다. 일반 판매 재고의 추정 소진율과 커버리지는 출고 영업일 기준이며,
소비기한은 달력일 기준이다. 발주 리드타임 없이 커버리지로 사람이 판단한다. B2B는 일괄 출고
품목이므로 반복 수요 예측에서 제외한다. SKU의 목록 이탈은 확정 품절이 아니며 현재 수량·자산을
알 수 없는 상태다. 로트별 소비기한은 있지만 로트별 잔량은 없다. 상세 산식은 README의 운영
지표 기준을 따른다. 이전 문서의 소비기한 무조건 비위험/목록 이탈 확정 품절 가정은 적용하지 않는다.
